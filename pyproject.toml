[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "investment_visualizer"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["infra*", "pipelines*", "app*"]